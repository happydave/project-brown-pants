# Material attribution

PBR material sets in this directory are baked outputs from the **asset-harness**
`pbr-materials` track (generated locally; albedo via Z-Image, maps derived locally). The
workflow/seed/prompt is the source of truth in the asset-harness repo; these PNGs are the
cached, versioned output.

License: Apache-2.0 / MIT (per the generating models' terms recorded in the asset-harness
track docs).

| Set | Maps | Used by |
| --- | ---- | ------- |
| `hull_panel` | albedo, normal, metallic_roughness, occlusion | shared / fallback craft skin (`-- skins`), WI 582/583 |
| `rocky_ground` | albedo, normal, metallic_roughness, occlusion | textured ground (`-- skins`/`-- play`/`-- autopilot`/`-- launch`), WI 588 |
| `aluminium_panel` | albedo, normal, metallic_roughness, occlusion | structural material **aluminium** (brushed aluminium panel), WI 624 |
| `steel_plate` | albedo, normal, metallic_roughness, occlusion | structural material **steel** (polished steel plate), WI 624 |
| `titanium_panel` | albedo, normal, metallic_roughness, occlusion | structural material **titanium** (satin titanium alloy), WI 624 |
| `carbon_weave` | albedo, normal, metallic_roughness, occlusion | structural material **composite** (carbon-fibre weave), WI 624 |

| `grass` | albedo, normal, metallic_roughness, occlusion | biome `grassland`, WI 871 (consumed by B5 splatting) |
| `steppe` | albedo, normal, metallic_roughness, occlusion | biomes `savanna`/`tundra`, WI 871 |
| `sand` | albedo, normal, metallic_roughness, occlusion | biomes `beach`/`desert`, WI 871 |
| `mud` | albedo, normal, metallic_roughness, occlusion | biome `swamp`, WI 871 |
| `forest_floor` | albedo, normal, metallic_roughness, occlusion | biomes `forest`/`taiga`, WI 871 |
| `rock` | albedo, normal, metallic_roughness, occlusion | biomes `highland`/`alpine`/`bedrock`/`bedrock outcrop`, WI 871 |
| `snow` | albedo, normal, metallic_roughness, occlusion | biomes `ice cap`/`cold-trap ice`, WI 871 |
| `regolith_fine` | albedo, normal, metallic_roughness, occlusion | biomes `regolith plains`/`bright highlands`, WI 871 |
| `regolith_coarse` | albedo, normal, metallic_roughness, occlusion | biome `boulder fields`, WI 871 |
| `basalt` | albedo, normal, metallic_roughness, occlusion | biome `maria`, WI 871 |

The four structural sets (WI 624) were generated by the `pbr-materials` track driver
`prototypes/gen_material_library.py` (seed 424242, 1024², albedo via Z-Image Turbo on `ai2`, maps
derived locally with `derive_pbr.py`). Bonus planetary-surface sets (`sand_dune`, `ice_sheet`,
`basalt_rock`) from the same run are banked in the asset-harness library, not yet imported here.

The ten terrain sets (WI 871, the Sounding biome layer's `texture_set` names) come from the same
driver's terrain section: per-set seeds/prompts in the harness `materials_manifest.json`, albedo
tone-balanced onto the consuming biomes' tint means (`derive_pbr.tone_balance`), tiling audited at
1×/3×3/8×8 (`prototypes/audit_tiling.py`; sheets in the track's findings). License chain unchanged:
Z-Image Turbo (Apache-2.0) + local deterministic derivation. Nothing loads them until B5 (WI 872);
until then biomes render by tint fallback.
